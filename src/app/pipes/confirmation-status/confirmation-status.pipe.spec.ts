import {ConfirmationStatusPipe} from './confirmation-status.pipe';

describe('ConfirmationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
